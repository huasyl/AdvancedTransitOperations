# RailEta Contracts 维护边界

- `Contracts.cs` 定义快照、请求、结果和回放使用的数据契约，由主 DLL 统一编译。
- 内置模块和热模块必须复用主 DLL 中的同一套契约类型；热模块工程禁止再次编译本目录。
- 字段时间统一使用模拟帧；本层不持有运行时状态，也不引用 ECS、主模组 owner 或文件系统。
- 这里不定义预测器接口或计算入口；唯一权威实现位于 `Core/RailEta`。
- 数据契约只服务请求帧冻结、结果发布和离线回放，不保留旧自由运行引擎、理论轨迹或延迟拆分合同。
